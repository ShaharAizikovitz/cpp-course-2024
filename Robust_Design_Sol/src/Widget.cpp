#include "Widget.h"
#include <sstream>

using namespace std;

string Widget::print() const {
	// Use Range-for to iterate the vector.
	// Use auto to type the element variable.
	// Stream the integer elements into an ostringstream.
	// Call .str() on the ostringstream to generate a string.
	// Your code here ...

	return "something";
}